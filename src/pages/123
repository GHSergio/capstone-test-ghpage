// // 調用 PodcastContext 中的 setPodcastList 函式更新 PodcastList 中的狀態
// setPodcastList((prevPodcastList) => {
//   return prevPodcastList.map((podcast) => {
//     if (podcast.title === video.title) {
//       return { ...podcast, isFavorite: true };
//     }
//     return podcast;
//   });
// });
